all:
  vars:
    # The generated inventory is assumed to be used from the clab host.
    # Hence no http proxy should be used. Therefore we make sure the http
    # module does not attempt using any global http proxy.
    ansible_httpapi_use_proxy: false
  children:
    linux:
      hosts:
        clab-scenario-hierarchy-E1:
          ansible_host: 
        clab-scenario-hierarchy-RR1S:
          ansible_host: 
        clab-scenario-hierarchy-RR1T:
          ansible_host: 
        clab-scenario-hierarchy-RR2S:
          ansible_host: 
        clab-scenario-hierarchy-RR2T:
          ansible_host: 
        clab-scenario-hierarchy-RR3S:
          ansible_host: 
        clab-scenario-hierarchy-RR4S:
          ansible_host: 
        clab-scenario-hierarchy-h2:
          ansible_host: 
        clab-scenario-hierarchy-h3:
          ansible_host: 